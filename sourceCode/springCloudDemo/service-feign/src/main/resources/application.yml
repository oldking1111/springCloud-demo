eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8765

spring:
  application:
    name: service-feign

# 自带断路器，默认是关闭
feign:
  hystrix:
    enabled: true